# Archived — incomplete Swift Package

This SPM executable target was an early native attempt. It is **superseded** by the full Xcode app:

```text
../macos/CyberActivity.xcodeproj
```

Open that project instead. Do not ship this package.
