# Electron prototype (secondary)

The primary CyberActivity product is the native Xcode app:

```text
../macos/CyberActivity.xcodeproj
```

This Electron stack (`electron/`, `renderer/`, root `package.json`) remains for reference and comparison only. Prefer the SwiftUI project for shipping and for exporting to another Mac.
